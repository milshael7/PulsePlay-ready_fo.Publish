# PulsePlay Deployment Instructions

## Prerequisites
- Node.js v20+ and npm installed
- PostgreSQL/MySQL (or DB configured in `database/schema.sql`)
- Optional: Docker for containerized deployment

## Backend Setup
1. Navigate to backend root: `/Pulseplay`
2. Install dependencies:
   ```bash
   npm install